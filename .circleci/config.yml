version: 2.1

jobs:
  deploy:
    docker:
      - image: cimg/node:19.2.0
    steps:
      - checkout
      
      - run:
          name: "TEST"
          command: echo $CIRCLE_PULL_REQUEST


workflows:
  version: 2
  push-workflow:
    jobs:
      - deploy
